# Frontend — Stock Analysis Web UI

Vite + React + TypeScript dashboard for the LangGraph backend.

## Setup

```powershell
copy .env.example .env
npm install
npm run dev
```

Default URL: http://localhost:8443  

Requires backend at http://localhost:8000.
